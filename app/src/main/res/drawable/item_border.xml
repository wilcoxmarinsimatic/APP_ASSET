<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Fondo blanco -->
    <corners android:radius="8dp" /> <!-- Bordes redondeados -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" /> <!-- Color del borde -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
