<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Botón de regresar -->
    <ImageButton
        android:id="@+id/btn_back_detail"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_back"
        android:background="@null"
        android:contentDescription="Regresar"
        android:layout_alignParentStart="true"
        android:layout_marginTop="16dp" />

    <!-- Título de la auditoría -->
    <TextView
        android:id="@+id/tv_auditoria_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Detalle de Auditoría"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />

    <!-- Detalle de la auditoría -->
    <TextView
        android:id="@+id/tv_auditoria_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_auditoria_title"
        android:layout_marginTop="32dp"
        android:text="Aquí se muestran los detalles de la auditoría seleccionada."
        android:textSize="16sp"
        android:textColor="#333333" />
</RelativeLayout>
